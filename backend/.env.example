# AWS credentials for deploy-time CLI (never commit real values)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=

# Runtime (set automatically by CloudFormation on Lambda; required locally for seed)
TABLE_NAME=
COGNITO_CLIENT_ID=
COGNITO_USER_POOL_ID=
